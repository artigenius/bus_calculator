# Четыре самостоятельных калькулятора Changellenge >>

Каждый HTML-файл открывает сразу свой калькулятор. Общего экрана выбора и переходов между калькуляторами нет.

| Калькулятор | Файл / путь на сайте |
|---|---|
| Стажировки | `internship.html` |
| Кейс-чемпионат для сотрудников | `growth.html` |
| EVP (разработка и активация) | `evp.html` |
| Сертификация | `certification.html` |

## Как пользоваться сейчас

Откройте нужный HTML-файл двойным щелчком в Finder. Если он открывается в редакторе, выберите «Открыть в программе» → Safari или Google Chrome. Интернет и запуск сервера не нужны. JavaScript в браузере должен быть включён.

Каждый файл содержит свою модель, оформление, шрифты и логотип. Его можно копировать в любую папку, переименовывать и отправлять отдельно — другие файлы проекта ему не нужны. При обновлении страницы восстанавливаются исходные значения, как и в прежней версии.

Архив `calculators-standalone.zip` содержит только четыре готовых HTML-файла и краткую инструкцию.

## Адреса в интернете

Страницы пока не опубликованы в интернете. Для публичных адресов загрузите четыре HTML-файла на свой статический хостинг или передайте администратору сайта. Если файлы лежат в одной папке сайта, адреса заканчиваются на `/internship.html`, `/growth.html`, `/evp.html`, `/certification.html`. Каждый адрес открывается напрямую и не требует главной страницы, маршрутизатора или настроек перенаправления на index.html.

Можно разместить каждый файл на отдельном сайте, переименовав его в `index.html`. Тогда у каждого калькулятора будет свой домен или поддомен. Домен определяется выбранным хостингом.

Старый `calculator.html` перенаправляет на «Стажировки». Старый `championship.html` перенаправляет на новый четвёртый продукт — «Сертификация»; прежний калькулятор внешних чемпионатов исключён из активного набора. Эти перенаправления работают, когда целевые страницы лежат рядом, и не входят в автономный архив.

## Обновление модели от 18 сентября 2026

Источник — `source_files/3_2026_financial_benefits.xlsx`, копия предоставленного файла «2026-Финансовая выгода для клиентов .xlsx». Используется лист **«Эконом. эффективность 2026 »** (с пробелом в конце), на котором перечислены именно эти четыре продукта. Другие версии расчётов из книги не смешиваются с ним.

| Продукт | Эффект, ₽ | Затраты, ₽ | ROI по модели |
|---|---:|---:|---:|
| Стажировки | 389 731 200,00 | 25 664 000,00 | 1 418,59% |
| Кейс-чемпионат для сотрудников | 63 978 034,29 | 8 946 285,71 | 615,14% |
| EVP (разработка и активация) | 95 013 000,00 | 22 000 000,00 | 331,88% |
| Сертификация | 1 005 312 000,00 | 2 348 000,00 | 42 715,67% |

Это результаты исходных формул при значениях по умолчанию, а не гарантия экономии. Кейс-чемпионат и сертификация объединяют эффекты разных периодов; в интерфейсе они не названы однородной годовой оценкой. Две стоимости кейс-чемпионата — 16 млн ₽ для R&D и 6 млн ₽ для затрат — сохранены по подтверждению пользователя. Полный разбор формул и противоречий: `docs/model-2026-update.md`.

## Изменения в коде

- `src/calculator.template.html` — общий исходный шаблон оформления и интерфейса; не предназначен для непосредственного открытия.
- `src/products/*.js` — четыре раздельные модели с обновлёнными формулами и исходными значениями.
- `scripts/build_pages.py` — сборка четырёх автономных HTML-файлов, совместимого перенаправления и архива; достаточно Python 3, дополнительные библиотеки не нужны.
- `scripts/extract_reference.py` — извлечение сохранённых числовых результатов и гиперссылок из исходного Excel без его изменения или пересчёта.
- `source_files/model-2026-reference.json` — эталонные значения Excel и ссылки на источники, используемые сборкой и тестами.
- `scripts/verify_models.cjs` — проверка отдельных эффектов, итогов, затрат и ROI по Excel; проверка зависимостей, индекса EVP и граничных условий. Требуется только Node.js.
- `scripts/verify_pages.cjs` — актуальная проверка через Playwright: прямой вход через файл и HTTP, эталонные расчёты, изменение полей, сброс, раскрытие деталей, перезагрузка, мобильная ширина и отсутствие внешних запросов.

После правок исходников выполните:

```sh
python3 scripts/build_pages.py
```

Для проверки, если установлены Node.js, Playwright и Chromium:

```sh
node scripts/verify_models.cjs
node scripts/verify_pages.cjs
```

При необходимости укажите путь к браузеру в `CHROME_PATH`, а к установленному модулю Playwright — через `NODE_PATH`.

После замены исходного Excel эталон обновляется командой `python3 scripts/extract_reference.py`. Изменения формул нужно отдельно перенести в модели и проверить; сборка не исполняет Excel-формулы автоматически.

Старые `scripts/verify*.py` и документация в `docs/project-history.md` относятся к предыдущей версии с общим экраном и сохранены как история. Для новой версии используйте `verify_pages.cjs`.

Исходные Excel-модели и материалы бренда сохранены в `source_files/` и `brand_assets/`.
